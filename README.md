# MiniBash – A Simple Unix Shell in C

MiniBash is a minimal Unix-like shell implemented in C to demonstrate how
`fork()`, `exec()`, and `wait()` work together to execute commands.

This project is meant for learning **Operating Systems** concepts, not for
production use.
